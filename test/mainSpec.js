define(['stringCalc'], function(stringCalc, _) {

    describe('That stringCalc', function() {

        it('exists in global scope', function() {
          expect(stringCalc).toBeTruthy();
        });

    });

});